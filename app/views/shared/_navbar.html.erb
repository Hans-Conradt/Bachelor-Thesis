<div class="<%= params[:controller] == 'pages' && params[:action] == 'home' ? "navbar navbar-expand-sm navbar-light navbar-lewagon fixed-top" : "navbar navbar-expand-sm navbar-light navbar-lewagon fixed-top navbar-lewagon-solid" %>">
  <%= link_to root_path, class: "navbar-brand" do %>
    <%= image_tag "Logo-fischertechnik.png" %>
  <% end %>
  <button class="navbar-toggler" type="button" data-toggle="collapse" data-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
    <span class="navbar-toggler-icon"></span>
  </button>


  <div class="collapse navbar-collapse" id="navbarSupportedContent">
    <ul class="navbar-nav mr-auto">
        <li class="nav-item">

        </li>
        <li class="nav-item dropdown">
            <%= image_tag("hsg.png", class: "avatar dropdown-toggle", id: "navbarDropdown", data: { toggle: "dropdown" }, 'aria-haspopup': true, 'aria-expanded': false) %>
         <% if user_signed_in? %>
          <div class="dropdown-menu dropdown-menu-right" aria-labelledby="navbarDropdown">
            <%= link_to "Log out", destroy_user_session_path, method: :delete, class: "dropdown-item" %>
          </div>
        </li>
      <% else %>
        </li>
      <% end %>
    </ul>
  </div>
</div>
